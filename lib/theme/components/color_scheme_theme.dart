import 'package:flutter/material.dart';
import '../util/theme_constants.dart';

final colorSchemeTheme = ColorScheme(
  primary: CoreColorScheme().primary,
  secondary: CoreColorScheme().secondary,
  surface: CoreColorScheme().surface,
  background: CoreColorScheme().background,
  error: CoreColorScheme().error,
  onPrimary: CoreColorScheme().onPrimary,
  onSecondary: CoreColorScheme().onSecondary,
  onSurface: CoreColorScheme().onSurface,
  onBackground: CoreColorScheme().onBackground,
  onError: CoreColorScheme().onError,
  brightness: Brightness.dark,
);
